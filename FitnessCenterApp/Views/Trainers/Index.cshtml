@model IEnumerable<FitnessCenterApp.Models.Trainer>

@{
    ViewData["Title"] = "Antrenör Yönetimi";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-white">Antrenör Listesi</h1>
        <p>
            <a asp-action="Create" class="btn btn-success rounded-pill">
                <i class="bi bi-plus-circle"></i> Yeni Antrenör Ekle
            </a>
        </p>
    </div>

    <div class="table-responsive">
        <table class="table table-hover shadow-lg">
            <thead class="table-dark">
                <tr>
                    <th>Antrenör Adı</th>
                    <th>Uzmanlık Alanı</th>
                    <th>Verdiği Hizmet</th>
                    <th class="text-end">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="fw-bold text-dark">@item.FullName</td>
                        <td class="text-dark">@item.Specialization</td>
                        <td class="text-dark">@(item.Service?.Name ?? "Belirtilmemiş")</td>
                        <td class="text-end">
                            <div class="btn-group" role="group">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-dark">Düzenle</a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-info">Detay</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">Sil</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>